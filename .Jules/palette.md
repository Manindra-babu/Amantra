# Palette's Journal

## 2024-05-22 - Accessibility in Dynamic Menus
**Learning:** Dropdowns often miss `aria-expanded` state toggling, confusing screen reader users about the menu's state.
**Action:** Always pair visual toggles with `aria-expanded` updates and ensure `Escape` key closes the menu for keyboard users.
